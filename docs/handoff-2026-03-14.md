# Handoff - 2026-03-14 - Orion & Gage

## 📌 Status Atual
- **Site Online:** Deploy concluído com sucesso no Vercel.
- **Design System:** Ativo e dinâmico via Sanity.
- **Segurança:** Patch CVE-2025-66478 aplicado (Next.js 15.2.6).

## 🛠️ Mudanças Críticas Realizadas
1.  **Arquitetura:** Unificação de Layout no `RootLayout`.
2.  **Design System:** Implementação de Tokens em `tailwind.config.ts` e variáveis CSS dinâmicas no `app/layout.tsx`.
3.  **Componentização:** Criação da molécula `PropertyCard`.
4.  **Infra:** Adição de `.npmrc` com `legacy-peer-deps=true` para mitigar conflitos de peer dependencies do Sanity v3.
5.  **Build:** Adição de `.vercelignore` para acelerar o deploy ignorando metadados AIOX.

## 🚀 Próximos Passos Sugeridos
1.  **Épico 2 - SEO Dinâmico:** Implementar `generateMetadata` para títulos e descrições vindos do Sanity.
2.  **Épico 3 - Filtros Avançados:** Tornar a busca e os filtros da listagem de imóveis funcionais via URL Params e GROQ dinâmico.
3.  **Infra - Planejamento Supabase:** Iniciar o design do banco de dados no Supabase para futura migração do Sanity.

## ⚠️ Notas DevOps
- Não remova o `.npmrc` até que a migração para o Supabase (ou atualização para Sanity v4+) seja concluída, pois ele sustenta a coexistência de Next.js 15 com dependências legadas.
- O arquivo `pnpm-lock.yaml` foi removido e regenerado pelo Vercel. Recomendamos rodar `pnpm install` localmente na volta para sincronizar o lockfile.

---
*Orquestrado por Synkra AIOX.*
